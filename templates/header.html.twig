

<nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
    <a class="navbar-brand" href="#">
        <img alt="Brand" src="{{ asset('assets/img/iconecap.svg') }}">
    </a>
    <a class="navbar-brand" href="#">SkillzBook</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <ul class="navbar-nav ml-auto">

        {% if app.user %}
            <li class="nav-item"> <a href="{{ path('account') }}">Mon compte </a> | <a href="{{ path('app_logout') }}"> Déconnexion</a> </li>
        {% else %}
            <li class="nav-item"> <a href="{{ path('account') }}">Connexion </a> | <a href="{{ path('register') }}"> Inscription</a> </li>
        {% endif %}
</nav>